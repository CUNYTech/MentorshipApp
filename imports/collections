import { Mongo } from 'meteor/mongo';

export const Users = new Mongo.Collection('users');

Meteor.methods({
  'users.insert': function() {
    return Users.insert({
      createdAt: new Date(),
      ownerId: this.userId
    });
  };

